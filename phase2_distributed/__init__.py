"""
AquaMind AI — Phase 2 (Distributed / Cloud-Scale Digital Twin).

This package extends Phase 1 (phase1_standalone/) rather than replacing it —
same data contracts, same API shapes, same database schema (extended, not
replaced). See docs/AquaMind_AI_SDD_Phase2.docx for the full design.
"""
