# Phase 2 uses everything Phase 1 uses (installed separately from
# phase1_standalone/requirements.txt) plus a couple of additions for the
# distributed extensions. Nothing here requires a paid service — the
# OpenDC/CloudSim adapters, multi-agent Orchestrator, and memory tiering
# job all run fully offline with zero mandatory cloud dependency, same as
# Phase 1 (FR-1.11 carried into Phase 2).

fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
pydantic==2.9.2
pydantic-settings==2.5.2
requests==2.32.3
httpx==0.27.2
pytest==8.3.3

# Optional (only needed when BEDROCK_ENABLED=true, same as Phase 1):
# boto3==1.35.0
